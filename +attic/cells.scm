;;@ Create a cell.
(define (make-cell contents) (proc-to-define))

;;@ Disjoint type predicate.
(define (cell? thing) (proc-to-define))

;;@ Return the current contents of @1.
(define (cell-ref cell) (proc-to-define))

;;@ Set the contents of @1 to @2.
(define (cell-set! cell value) (proc-to-define))
